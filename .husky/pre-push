current_branch=$(git symbolic-ref HEAD | sed -e 's,.*/\(.*\),\1,')

if [ "$current_branch" = "main" ]; then
    echo 'You are pushing to "main" branch. This is not allowed for protected branches.'
    exit 1
fi
